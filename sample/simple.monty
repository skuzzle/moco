class Simple:
    + ? bar := "hallO"
    
    + foo():
        ? x := shouldNotFail(5)
        ? xyz := "bla"
        ? y := shouldNotFail(xyz)
        //? i := x * bar

    // test
    + ? shouldNotFail(Int a):
        if a < 0:
            return 1
        else:
            return 2
            
    + ? shouldNotFail():
        return ""